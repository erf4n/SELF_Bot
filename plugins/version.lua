do

function run(msg, matches)
    return "🤖سلف بوت فارسی توسط 👈@decopaje"
end 
return {
  patterns = {
    "^(ربات)$",
    "^bot$",
    "^سلف بوت$",
  },
  run = run
}
end
